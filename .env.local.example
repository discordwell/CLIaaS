# Database (start Postgres with: docker compose up -d)
DATABASE_URL=postgresql://cliaas:cliaas@localhost:5433/cliaas

# Optional: specify workspace name (defaults to first workspace)
# CLIAAS_WORKSPACE=demo
