export const metadata = {
  title: 'WFM Dashboard | CLIaaS',
  description: 'Workforce management — scheduling, adherence, forecasting, and shift swaps.',
};

export default function WfmLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
