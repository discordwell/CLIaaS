export const metadata = {
  title: 'Channels | CLIaaS',
  description: 'Omnichannel management — monitor, configure, and administer all communication channels.',
};

export default function ChannelsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
